// Local default — safe to edit. Overrides VITE_API_URL at runtime.
// localStorage["conquest.apiUrl"] still wins (set via the login screen settings).
window.__CONQUEST_CONFIG__ = window.__CONQUEST_CONFIG__ || {};
